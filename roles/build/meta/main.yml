galaxy_info:
# argument_specs:
    role_name: build
    description: build monorepo
    author: Ars-Rem
    license: DEV
    min_ansible_version: 2.9
    platforms:
    - name: Ubuntu
      versions:
        - all

#dependencies:
#  - role: deploy
#allow_duplicates: true
