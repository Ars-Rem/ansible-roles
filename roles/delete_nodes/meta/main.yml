galaxy_info:
# argument_specs:
    role_name: delete_nodes
    description: delete nodes from cluster
    author: Ars-Rem
    license: DEV
    min_ansible_version: 2.9
    platforms:
    - name: Ubuntu
      versions:
        - all

#dependencies:
#  - role: deploy
#allow_duplicates: true
