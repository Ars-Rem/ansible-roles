docker node ls
docker node update server1 --label-add registry=true
docker node inspect server1
mkdir registry
docker service create --name registry --publish 5000:5000 --constraint node.labels.registry==true --mount type=bind,source=/home/vagrant/registry,destination=/var/lib/registry -e REGISTRY_HHT_ADDR=0.0.0.0:5000 registry:latest